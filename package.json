{
  "author": "Pedro André Oliveira",
  "bugs": {
    "url": "https://github.com/ei06125/js-template/issues"
  },
  "config": {
    "paths": {
      "root": "."
    }
  },
  "cpu" : [ "x64", "ia32" ],
  "contributors": [
    {
      "name": "Pedro André Oliveira",
      "email": "pedro.andre.oliveira@outlook.com"
    }
  ],
  "description": "A JavaScript template project",
  "engines": {
    "node": ">=10.17.0"
  },
  "files": [],
  "homepage": "https://github.com/ei06125/js-template#readme",
  "keywords": [
    "Project",
    "Template"
  ],
  "license": "MIT",
  "main": "index.js",
  "name": "js-template",
  "os": [ "win32", "linux", "darwin" ],
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ei06125/js-template.git"
  },
  "scripts": {
    "preconfig": "echo \"Run BEFORE the 'npm run config' command\"",
    "config": "echo \"configs the project\"",
    "postconfig": "echo \"Run AFTER the 'npm run config' command\"",
    "prebuild": "echo \"Run BEFORE the 'npm run build' command\"",
    "build": "echo \"Builds the project\"",
    "postbuild": "echo \"Run AFTER the 'npm run build' command\"",
    "pretest": "echo \"Run by the 'npm test' command\"",
    "test": "NODE_ENV=test mocha -recursive './**/*.spec.js'",
    "posttest": "echo \"Run by the 'npm test' command\"",
    "preinstall": "echo \"Run BEFORE the package is installed\"",
    "install": "echo \"run AFTER the package is installed\"",
    "postinstall": "echo \"run AFTER the package is installed\"",
    "preversion": "echo \"Run BEFORE bump the package version\"",
    "version": "echo \"run BEFORE bump version\"",
    "postversion": "echo \"Run AFTER bump the package version\""
  },
  "version": "0.0.1"
}
